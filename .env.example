# FreightX Environment Variables Example
# Duplicate this file to .env and configure the variables below.

# 1. Private key used for smart contract deployment on Arc Testnet (DO NOT use mainnet keys!)
# Ensure the account has testnet USDC for gas (since Arc uses USDC as the native gas token).
# Fund your wallet address at https://faucet.circle.com (select Arc Testnet).
PRIVATE_KEY=0xYOUR_TESTNET_PRIVATE_KEY_HERE

# 2. RPC URL for the Arc Testnet chain
# Default public RPC endpoint for Arc Testnet is: https://rpc.testnet.arc.network
ARC_TESTNET_RPC_URL=https://rpc.testnet.arc.network

# 3. Circle Developer Platform Configuration (Modular Wallets & Paymaster)
# Your public key for modular wallets (obtained from Circle Developer Console)
NEXT_PUBLIC_CIRCLE_CLIENT_KEY=
# Circle Client URL (Default for Testnet is: https://api.circle.com/v1/w3s)
NEXT_PUBLIC_CIRCLE_CLIENT_URL=https://api.circle.com/v1/w3s
# Circle Console API Key (kept secret, backend/server-side only if needed)
CIRCLE_API_KEY=

# 4. Supabase Configuration (Backend Database)
SUPABASE_URL=https://your-supabase-url.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# 5. Circle Gateway Nanopayments Configuration
GATEWAY_API_KEY=your-gateway-api-key
GATEWAY_SELLER_ADDRESS=0x9b1C51cEF8bc8757ad757845ef80A390a3b9d194

# 6. Circle StableFX Configuration
STABLEFX_API_KEY=YOUR_STABLEFX_API_KEY



